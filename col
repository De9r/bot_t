import telebot
to = input(" Entar Token : ")
bot=telebot.TeleBot(to)
@bot.message_handler(func=lambda m:True)
def main(message):
 if "كول " in message.text:
  rep=str(message.text).split("كول ")
  bot.send_message(message.chat.id,rep[1])
bot.polling()
